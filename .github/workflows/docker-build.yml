name: Build and Push Docker Image

# 定义触发工作流的条件
on:
  push:
    branches:
      - main  # 当代码推送到 main 分支时触发
  pull_request:
    branches:
      - main  # 当向 main 分支提交 PR 时触发，用于测试构建

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    # 授予推送镜像到 GHCR 所需的权限
    permissions:
      contents: read
      packages: write

    steps:
      - name: 检出仓库代码
        uses: actions/checkout@v4

      - name: 设置 Docker Buildx (用于高级构建和缓存)
        uses: docker/setup-buildx-action@v3

      - name: 登录到 GitHub 容器注册表 (GHCR)
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          # GITHUB_TOKEN 是 GitHub Actions 自动生成的，无需手动配置
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 登录到 Docker Hub
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          # 需要在 GitHub 仓库的 Secrets 中配置这两个变量
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: 提取 Docker 镜像的元数据 (标签和标签)
        id: meta
        uses: docker/metadata-action@v5
        with:
          # 同时指定 GHCR 和 Docker Hub 的镜像路径 (按您的 README 使用了 ubuntu-oss 作为镜像名)
          images: |
            ghcr.io/${{ github.repository }}
            docker.io/${{ secrets.DOCKERHUB_USERNAME }}/ubuntu-oss
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,format=short

      - name: 构建并推送 Docker 镜像
        uses: docker/build-push-action@v5
        with:
          context: .
          # 如果是 PR，则只构建不推送；如果是 push 到 main，则构建并推送
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          # 使用 GitHub Actions 缓存层以加速后续构建
          cache-from: type=gha
          cache-to: type=gha,mode=max
